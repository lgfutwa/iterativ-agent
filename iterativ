#!/usr/bin/env python3
"""
Iterativ Agent CLI launcher.

This wrapper should behave like the installed `iterativ` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from iterativ_cli.main import main
    main()
